const Home = () => {
  return (
    <div>
      home
      <p>
        Home is the default route of the application. It is the first page that
        the user sees when they visit the application. It is also the page that
        the user sees when they click on the home link in the navigation bar.
      </p>
    </div>
  );
};

export default Home;
